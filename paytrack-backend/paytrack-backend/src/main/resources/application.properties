spring.application.name=paytrack-backend

# Database
spring.datasource.url=${DB_URL:jdbc:mysql://gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/paytrack_db?sslMode=VERIFY_IDENTITY&enabledTLSProtocols=TLSv1.2,TLSv1.3}
spring.datasource.username=${DB_USERNAME:2WAWjQPYLRZ5DSB.root}
spring.datasource.password=${DB_PASSWORD:0XBFYDsIqMPt59BS}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# JWT
jwt.secret=${JWT_SECRET:paytrack_secret_key_change_in_production}
jwt.expiration=86400000